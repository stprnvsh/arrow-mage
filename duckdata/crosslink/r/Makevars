# Makevars file for CrossLink R package

# Define path to the pre-built C++ libraries relative to this file (r/Makevars)
CPP_BUILD_DIR = ../cpp/build

# Flags for the linker
# -L points to the directory containing the libraries
# -l links against the specific library (name without 'lib' prefix and '.dylib'/'.so' suffix)
PKG_LIBS = -L$(CPP_BUILD_DIR) -lcrosslink_r -lcrosslink

# Optional: If your C++ code used Rcpp, you might need Rcpp flags
# PKG_LIBS += $(shell $(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()")
# PKG_CPPFLAGS = -I"$(R_HOME)/library/Rcpp/include" $(shell $(R_HOME)/bin/Rscript -e "Rcpp:::CxxFlags()")

# Ensure C++17 is used if needed by your C++ code (might be set by Rcpp flags too)
# CXX17 = g++
# CXX17STD = -std=gnu++17
# CXX17FLAGS = $(CXXFLAGS) 